/**
 * Adds two numbers together to provide the sum of the numbers
 * @param a the first number
 * @param b the second number
 * @returns the sum of the two numbers added together
 */
export function sum(a: number, b: number) {
    return a + b;
}